import { Button } from '@heroui/button';

export const TestButton = () => {
  return (
    <Button
      href={'https://'}
      // as={}
      color="primary"
      variant="shadow"
      size="lg"
      // className="mt-12 text-sm sm:hidden"
      // fullWidth
    >
      Get started
    </Button>
  );
};
